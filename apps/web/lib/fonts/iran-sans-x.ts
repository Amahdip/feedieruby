import localFont from "next/font/local";

// Use font-variation-settings (e.g. "wght" 400–700) alongside or instead of font-weight for variable weights.
export const iranSansX = localFont({
  src: "../../public/fonts/iran-sans-x/IRANSansXV.woff2",
  variable: "--font-IRANSansX",
  display: "swap",
});
